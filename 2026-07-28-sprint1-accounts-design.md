# Sprint 1 設計思考：帳號模組（Accounts）

日期：2026-07-28
專案：AI 文件管理平台
本次目標：不寫程式碼，先想清楚帳號系統要怎麼設計（API 端點、資料庫、認證方式）

---

## 一、核心問題：伺服器怎麼「記住」使用者已經登入？

### HTTP 是「無狀態」的
每一次瀏覽器跟伺服器的請求，伺服器都當成全新、素不相識的陌生人在對待，不會自動記得上一次發生過什麼事。所以「登入後，下一個請求怎麼知道是同一個人」，需要額外的機制來解決。

**不能用 IP 判斷身份**：
- 同一間公司、同一個 WiFi 底下的人，常常共用同一個對外 IP（NAT），無法用來區分不同使用者
- IP 本身也會變動（換網路、換熱點），綁定 IP 會導致使用者莫名其妙被登出

### 兩種主流解法：Session vs Token（JWT）

| | Session | Token（JWT） |
|---|---|---|
| 運作方式 | 登入後伺服器建一筆記錄，發一組識別碼給瀏覽器（存在 Cookie）；之後每次請求，伺服器都要拿識別碼去查記錄 | 登入後伺服器直接發一張「已簽名蓋章、寫好身份」的通行證；之後每次請求，伺服器只要驗證簽名是否為真，不用查資料庫 |
| 伺服器要不要額外查詢 | 要（每次都要查記錄本） | 不用（自己看一眼簽名就知道） |
| 適合場景 | 傳統網頁（只透過瀏覽器操作） | API 服務、手機 App、前後端分離架構 |
| 多台伺服器擴充 | 較麻煩（要讓多台伺服器共享同一份記錄） | 簡單（任何一台伺服器拿到通行證都能自己驗證） |

**本專案選擇 Token（JWT）**，原因：我們在做 Django REST Framework 的 API 服務，未來可能有網頁前端、甚至手機 App 存取，JWT 不依賴伺服器端狀態，更適合這種多用戶端的架構。

**Token 實際運作流程（以手機 App 為例）**：
1. 第一次登入 → 輸入帳號密碼 → 伺服器驗證通過 → 發一張 Token 給 App
2. App 把 Token 存在手機本地（不是存在伺服器）
3. 之後每次跟伺服器要資料，App 自動把存好的 Token 一起送出
4. 伺服器驗證 Token 的簽名是真的 → 直接放行，不用重新輸入密碼
5. Token 有效期限到了 → 系統要求重新登入，拿一張新的 Token（這是刻意的安全機制，避免 Token 永久有效、一旦外流就永遠能冒充）

---

## 二、把 User Story 轉換成具體的 API 端點

### 判斷 GET 還是 POST 的原則
> 這個動作，是「要資料」還是「動資料」？
> - 要資料（查看、搜尋、瀏覽）→ `GET`
> - 動資料（新增、修改、登入這種需要送機密資訊的）→ `POST`

**為什麼登入不能用 GET**：GET 請求的參數會直接顯示在網址列上，也會被瀏覽器歷史紀錄、伺服器 log 記錄下來。如果登入用 GET，帳號密碼會變成明文寫在網址裡，非常不安全。所以登入、註冊這類涉及機密資料的動作，一律用 `POST`（資料放在請求本體 body 裡傳送，不會顯示在網址上）。

### 帳號模組的完整 API 清單

```
POST /api/accounts/register/   ← 新增使用者（建立帳號）
POST /api/accounts/login/      ← 驗證帳號密碼，成功後發一張 Token 回來
GET  /api/accounts/me/         ← 查看「目前登入的人」自己的基本資料
```

`GET /api/accounts/me/` 是企業實務上幾乎一定會有的 API：前端頁面載入時，常常需要知道「現在是誰登入的、顯示他的名字/頭像」，這支 API 就是拿著 Token 去問「我是誰」。

---

## 三、資料庫層面的決策：要不要擴充 Django 內建的 User model？

### 為什麼不要自己從零設計使用者資料表
Django 內建的 User 表，已經處理好很多不該自己重新發明的東西：
- 密碼**加密儲存**的邏輯（密碼加密是資安專業領域，自己手刻很容易出漏洞）
- 登入驗證的底層機制
- 跟 Django 其他內建功能（如後台 Admin）的相容性

### 採用「Profile 模式」（擴充檔案模式）
保留 Django 內建的 User 表，另外新增一張 `UserProfile` 表，存放額外資訊，兩張表用「一對一（One-to-One）」關係連接。

**比喻**：User 表是「身分證」（只記錄最核心的身份資訊：帳號、密碼、信箱）；UserProfile 表是「個人檔案夾」（記錄職業等額外資訊）。兩者用身分對應起來，一個人對應一份檔案夾。

```
User (Django 內建)
  - id
  - username
  - email
  - password (加密儲存)

UserProfile (自己新增)
  - id
  - user (一對一連到 User 的 id)
  - job_title (職業，選填)
  - created_at
```

### 一對一（One-to-One）vs 一對多（One-to-Many）的判斷原則
判斷關係類型的方法：想「現實生活中，這兩個東西的關係，合不合理是『一個對一個』，還是『一個對很多個』」，資料庫設計要忠實反映現實邏輯。

- `User ── UserProfile`：一對一。一個人現實中就是一個人，只該對應一份個人檔案，不會同時有兩份互相矛盾的檔案。
- `User ──< Document`：一對多。一個使用者現實中本來就可以擁有多份文件，這是合理的。

---

## 四、認證方式的技術選型

Django 生態圈實作 JWT 最主流的套件：**`djangorestframework-simplejwt`**（django-rest-framework 的 simple-jwt，意思是「幫 DRF 這個框架簡單地加上 JWT 功能」）。

企業實務上幾乎不會自己從零手刻 JWT 的加密邏輯，跟「密碼加密不要自己刻」是同樣的道理——安全性相關的底層邏輯，交給經過廣泛驗證的現成套件。

之後寫程式碼時，這個套件會接上 `POST /api/accounts/login/` 這支 API：送帳號密碼過去，套件自動驗證、自動產生 Token 回傳，不用自己寫加密演算法。

---

## 五、今天想通的四件事（總結，可直接用於面試回答）

1. HTTP 是無狀態的，伺服器不會自動記得使用者是誰，需要 Session 或 Token 這類額外機制
2. 選擇 Token（JWT）而非 Session，因為本專案是 API 服務，未來可能有多種用戶端（網頁、App）存取
3. API 設計要分清楚「要資料」用 GET，「動資料」（含機密資訊）用 POST
4. 資料庫善用 Django 內建的 User，用「一對一」外掛 UserProfile 表，不自己重造密碼加密這種危險的輪子

---

## 六、下次動手寫程式碼的順序（Sprint 1 實作）

設計都想清楚了，下次可以直接照著這份清單動手：

1. 建立 `accounts` 這個 Django App
2. 安裝 `djangorestframework` 與 `djangorestframework-simplejwt`
3. 建立 `UserProfile` model，跑 migration
4. 實作 `POST /api/accounts/register/`
5. 實作 `POST /api/accounts/login/`（串接 simplejwt）
6. 實作 `GET /api/accounts/me/`
7. 用 Postman 或類似工具，逐一測試三支 API
